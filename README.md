events-based parallax

`npm install && grunt build`

### instantiation

```
var scroller = new Scroller();
var parallax = new Parallax({
    elements: [
	    {
	        element: document.getElementById('block1'),
	        speed: 0.5
	    }
    ]
);
```

